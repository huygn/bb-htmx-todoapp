{
  "name": "htmlx-cljs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "deno": "deno run -A --unstable --watch index.js",
    "clean": "rm -rf dist",
    "dev": "shadow-cljs watch app",
    "build": "shadow-cljs compile app",
    "release": "shadow-cljs release app"
  },
  "devDependencies": {
    "husky": "^7.0.2",
    "shadow-cljs": "2.15.8"
  }
}
